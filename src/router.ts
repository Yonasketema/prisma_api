import { Router } from "express";

const router = Router();

router.get("/product", () => {});
router.post("/product", () => {});
router.put("/product/:id", () => {});
router.get("/product/:id", () => {});
router.delete("/product/:id", () => {});

/**
 * Update
 */
